{
  "name": "document-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --cwd=apps/$APP run dev",
    "start": "bun --cwd=apps/$APP run start"
  },
  "dependencies": {}
}
